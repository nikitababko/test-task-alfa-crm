import Search from 'components/Search';
import React from 'react';

import { Card } from 'UI';

const Home = () => {
  return (
    <div>
      <Search />
      <Card />
    </div>
  );
};

export default Home;
