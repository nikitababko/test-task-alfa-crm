import React from 'react';

import Search from 'components/Search';

import { Card } from 'UI';

const Home = () => {
  return (
    <div>
      <Search />
      <Card />
    </div>
  );
};

export default Home;
